# Optional alternative to Windows Credential Manager.
# Never commit a real key. Copy this file outside the repository or set the
# environment variable in your shell/session instead.
OPENAI_API_KEY=
